apps:
  - script: ./bin/www
    name: obojobo
    exec_mode: cluster
    instances: 2
    cwd: ../../current
    max_memory_restart: 100M # I think 100 should be plenty
    env:
      NODE_ENV: production
