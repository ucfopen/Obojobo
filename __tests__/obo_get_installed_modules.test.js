import getInstalledModules from '../obo_get_installed_modules'

describe('Get Installed Modules', () => {
	test.skip('getInstalledModules runs without an error', () => {
		getInstalledModules('test')
	})
})
