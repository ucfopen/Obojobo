import './focus-blocker.scss';

import FocusUtil from '../../common/util/focus-util';

let FocusBlocker = React.createClass({

	render() {
		return <div className='viewer--components--focus-blocker' />;
	}
});

export default FocusBlocker;
