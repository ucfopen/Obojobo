require './errordialog.scss'

SimpleDialog = require './simpledialog'

module.exports = React.createClass
	render: ->
		`<div className="obojobo-draft--components--modal--error-dialog">
			<SimpleDialog ok title={this.props.title}>
				{this.props.children}
			</SimpleDialog>
		</div>`