require './button.scss'

module.exports = React.createClass
	getDefaultProps: ->
		value: null
		disabled: false
		align: 'center'

	focus: ->
		ReactDOM.findDOMNode(@refs.button).focus()

	render: ->
		if @props.value
			children = @props.value
		else
			children = @props.children

		`<div className={"obojobo-draft--components--button" + (this.props.dangerous ? ' dangerous' : '') + (this.props.altAction ? ' alt-action' : '') + (' align-' + this.props.align)}>
			<button
				ref='button'
				onClick={this.props.onClick}
				tabIndex={this.props.shouldPreventTab ? '-1' : this.props.tabIndex}
				disabled={this.props.disabled || this.props.shouldPreventTab}
			>{children}</button>
		</div>`