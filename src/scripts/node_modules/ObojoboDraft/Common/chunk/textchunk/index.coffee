module.exports = React.createClass
	getDefaultProps: ->
		indent: 0

	render: ->
		`<div className={'text-chunk' + (this.props.className ? ' ' + this.props.className : '')}>
			{this.props.children}
		</div>`