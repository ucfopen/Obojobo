class VirtualCursor
	constructor: (@chunk, @data) ->

	isEquivalentTo: (otherCursor) ->
		@chunk.areCursorsEquivalent @, otherCursor

	clone: ->
		# @chunk.cloneVirtualCaret @
		new VirtualCursor @chunk, Object.assign({}, @data)


module.exports = VirtualCursor