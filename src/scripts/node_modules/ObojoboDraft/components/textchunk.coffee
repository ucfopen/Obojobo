module.exports = React.createClass
	getDefaultProps: ->
		indent: 0

	render: ->
		`<div className={'text-chunk' + (this.props.className ? ' ' + this.props.className : '')} data-margin={this.props.indent}>
			{this.props.children}
		</div>`