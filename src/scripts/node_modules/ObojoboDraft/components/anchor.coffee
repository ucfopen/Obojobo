EMPTY_CHAR = require('ObojoboDraft/text/textconstants').EMPTY_CHAR

module.exports = React.createClass
	render: ->
		`<span
			{...this.props}
			className={'anchor' + (this.props.focusOutline ? ' focus-outline' : ' no-focus-outline')}
			contentEditable="true"
			suppressContentEditableWarning={true}
			data-group-index={'anchor:' + this.props.name}
		>
			{EMPTY_CHAR}
		</span>`