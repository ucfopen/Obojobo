require './viewer-component.scss'

Common = window.ObojoboDraft.Common
OboComponent = Common.components.OboComponent


module.exports = React.createClass
	render: ->
		childEl = null
		if @props.moduleData.navState.navTarget?.value?
			child = @props.model.getChildContainingModel @props.moduleData.navState.navTarget.value
			ChildComponent = child.getComponentClass()
			childEl = `<ChildComponent model={child} moduleData={this.props.moduleData} />`

		`<OboComponent
			model={this.props.model}
			className="obojobo-draft--sections--content"
		>
			<div>
				{childEl}
			</div>

		</OboComponent>`