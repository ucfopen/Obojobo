ObojoboDraft = window.ObojoboDraft

OBO.register 'ObojoboDraft.Sections.Content', {
	type: 'section',
	default: true,
	adapter: null,
	componentClass: require('./viewer-component'),
	selectionHandler: null,
	generateNav: (model) ->
		nav = []

		for child, index in model.children.models
			nav.push {
				type: 'link',
				label: child.title
				id: child.get('id')
			}

		nav.push {
			type: 'seperator'
		}

		nav
}