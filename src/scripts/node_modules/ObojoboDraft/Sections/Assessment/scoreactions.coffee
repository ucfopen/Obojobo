class ScoreActions
	constructor: (@_actions = []) ->

	getActionForScore: (score) ->
		for action in @_actions
			if score >= action.from and score <= action.to then return action

		null

	toObject: ->
		Object.assign [], @_actions

	clone: ->
		new ScoreActions @toObject()


module.exports = ScoreActions