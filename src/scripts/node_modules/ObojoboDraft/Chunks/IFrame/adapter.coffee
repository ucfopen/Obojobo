Adapter =
	construct: (model, attrs) ->
		if attrs?.content?.src?
			model.modelState.src = src
		else
			model.modelState.src = null

	clone: (model, clone) ->
		clone.modelState.src = model.modelState.src

	toJSON: (model, json) ->
		json.content.src = model.modelState.src


module.exports = Adapter