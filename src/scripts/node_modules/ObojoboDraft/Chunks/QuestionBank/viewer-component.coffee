require './viewer-component.scss'

Common = window.ObojoboDraft.Common

OboComponent = Common.components.OboComponent

QuestionBank = React.createClass
	getInitialState: ->
		children = @props.model.children.models
		child = children[Math.floor(Math.random() * children.length)]

		child: child

	render: ->
		child = @state.child
		Component = child.getComponentClass()

		`<OboComponent
			model={this.props.model}
			className="obojobo-draft--chunks--question-bank"
		>
			<button className="reshuffle" onClick={this.onClick}>Reshuffle</button>
			<Component model={child} />
		</OboComponent>`

	onClick: ->
		this.forceUpdate();

module.exports = QuestionBank