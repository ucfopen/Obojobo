require './viewer-component.scss'

Common = window.ObojoboDraft.Common

OboComponent = Common.components.OboComponent
Button = Common.components.Button
TextGroupEl = Common.chunk.textChunk.TextGroupEl
TextChunk = Common.chunk.TextChunk

ActionButton = React.createClass
	onClick: ->
		@props.model.processTrigger 'onClick'

	render: ->
		textItem = @props.model.modelState.textGroup.first

		`<OboComponent model={this.props.model} moduleData={this.props.moduleData}>
			<TextChunk className="obojobo-draft--chunks--action-button pad">
				<Button onClick={this.onClick} value={this.props.model.modelState.label}>
					<TextGroupEl text={textItem.text} groupIndex="0" indent={textItem.data.indent} parentModel={this.props.model} />
				</Button>
			</TextChunk>
		</OboComponent>`

module.exports = ActionButton