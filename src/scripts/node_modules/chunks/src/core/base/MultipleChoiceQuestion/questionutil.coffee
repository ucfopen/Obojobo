
getCorrectAnswers = (textGroup) ->
	correct = []
	for textItem in textGroup.items
		if textItem.data.score and textItem.data.score is 100
			correct.push textItem

	correct

module.exports = {
	getCorrectAnswers: getCorrectAnswers
}