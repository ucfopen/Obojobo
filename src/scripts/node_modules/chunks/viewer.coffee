requireAll = (requireContext) -> requireContext.keys().map(requireContext)
modules = requireAll(require.context('./src', true, /\viewer.coffee$/))

exports = {}
for m in modules
	exports["ObojoboDraft.Chunks.#{m.displayName}"] = m

module.exports = exports