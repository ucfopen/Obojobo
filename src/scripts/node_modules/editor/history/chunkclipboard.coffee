Chunk = ObojoboDraft.models.Chunk

class ChunkClipboard
	constructor: ->
		@clear()

	clear: ->
		@chunks = {}

	store: (chunk) ->
		@chunks[chunk.get('id')] = chunk.toJSON()

	get: (id) ->
		if not @chunks[id] then return null
		new Chunk @chunks[id]


module.exports = ChunkClipboard